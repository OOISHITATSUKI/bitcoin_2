[build]
  command = "npm run build"
  publish = "build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  CI = "false" 

interface BinanceApiClient {
  getBalance: (symbol: string) => Promise<any>;
  getPrice: (symbol: string) => Promise<any>;
} 